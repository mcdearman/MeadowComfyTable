-- comfyTable: tables for the terminal, with borders, alignment, colours, and
-- content wrapped to fit a width. A port of Rust's `comfy-table` crate,
-- version 8.0.0.
--
--   use ComfyTable (table, setHeader, addRow, rowOf, render)
--
--   render (table |> setHeader (rowOf ["Name", "Age"]) |> addRow (rowOf ["Ada", "36"]))
--     -- +------+-----+
--     -- | Name | Age |
--     -- +============+
--     -- | Ada  | 36  |
--     -- +------+-----+

mod Style
mod Cell
mod Table
mod Split
mod Layout
mod Format
mod Render
@cfg(test)
mod Cases
@cfg(test)
mod Tests

@pub use ComfyTable.Style
@pub use ComfyTable.Style.CellAlignment.*
@pub use ComfyTable.Style.Width.*
@pub use ComfyTable.Style.ColumnConstraint.*
@pub use ComfyTable.Style.ContentArrangement.*
@pub use ComfyTable.Style.Color.*
@pub use ComfyTable.Cell
@pub use ComfyTable.Table
@pub use ComfyTable.Split (measureTextWidth)
@pub use ComfyTable.Render
