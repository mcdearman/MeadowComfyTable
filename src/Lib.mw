-- comfyTable: tables for the terminal, with borders, alignment, colours, and
-- content wrapped to fit a width. A port of Rust's `comfy-table` crate,
-- version 8.0.0.
--
--   use Picnic (table, setHeader, addRow, rowOf, render)
--
--   render (table |> setHeader (rowOf ["Name", "Age"]) |> addRow (rowOf ["Ada", "36"]))
--     -- +------+-----+
--     -- | Name | Age |
--     -- +============+
--     -- | Ada  | 36  |
--     -- +------+-----+

mod Style
mod Cell
mod Table
mod Split
mod Layout
mod Format
mod Render
@cfg(test)
mod Cases
@cfg(test)
mod Tests

@pub use Picnic.Style
@pub use Picnic.Style.CellAlignment.*
@pub use Picnic.Style.Width.*
@pub use Picnic.Style.ColumnConstraint.*
@pub use Picnic.Style.ContentArrangement.*
@pub use Picnic.Style.Color.*
@pub use Picnic.Cell
@pub use Picnic.Table
@pub use Picnic.Split (measureTextWidth)
@pub use Picnic.Render
