[package]
name = "generate"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
comfy-table = "=8.0.0"
# The version comfy-table styles with, named so that its colour switch can be
# set: the generator must not heed `NO_COLOR`.
crossterm = { version = "=0.29.0", default-features = false }

[build-dependencies]
serde_json = "1"
